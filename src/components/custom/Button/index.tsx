import * as React from 'react';
import styles from './styles.module.scss';

interface IComponentProps {
    
}

export default function Button(props: IComponentProps) {
    return (
        <div className={styles.root}>
            
        </div>
    )
}

